{
  "name": "cyats",
  "version": "1.0.0",
  "description": "Exercise cypress with BDD",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "cypress:execution": "cypress run --spec cypress/e2e/features/*",
    "cypress:execution-tags": "cypress run --env tags=@tag"
  },
  "author": "gonatr",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^18.0.6",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cypress-mochawesome-reporter": "^3.5.1"
  },
  "devDependencies": {
    "cypress": "^13.6.3"
  }
}
